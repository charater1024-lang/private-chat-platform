name: key_transparency
description: Privacy-preserving reference primitives for append-only key transparency.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
