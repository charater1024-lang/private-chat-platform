name: homeserver_runtime
description: Loopback-only reference runtime for an opaque E2EE homeserver.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.2

resolution: workspace

dependencies:
  chat_core: ^0.1.0
  chat_media: ^0.1.0
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
