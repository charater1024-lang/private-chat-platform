Reserved for optional legacy 3-column by 2-row sprite sheets.

The intended signature asset set currently keeps this directory empty. It uses
the six transparent PNG cut-outs from the parent directory and composes them
at runtime (`usesSpriteSheet: false`) to avoid duplicate image decodes.
