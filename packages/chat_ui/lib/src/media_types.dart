/// Media kinds supported by shared chat presentation widgets.
enum ChatMediaKind { image, video, file }

/// Presentation-only transfer state for a media message.
enum MediaMessageStatus { sent, sending, failed }
